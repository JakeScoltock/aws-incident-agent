name: Deploy

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ secrets.AWS_ROLE_ARN != '' }}
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ap-southeast-2

      - name: Login to ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build investigator agent image
        run: |
          # TODO: build and push investigator agent image
          echo "Investigator image build not yet implemented"

      - name: Build remediation agent image
        run: |
          # TODO: build and push remediation agent image
          echo "Remediation image build not yet implemented"

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.12.0"

      - name: Deploy infrastructure
        run: |
          # TODO: terraform init + apply
          echo "Terraform apply not yet implemented"
